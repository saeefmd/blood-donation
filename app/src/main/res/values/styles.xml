<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/baseColor</item>
        <item name="colorPrimaryDark">@color/upperColor</item>
        <item name="colorAccent">@color/baseColor</item>
    </style>

    <style name="user_info_text">
        <item name="android:textSize">14sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">#000000</item>
    </style>

    <style name="info_box_style">
        <item name="android:background">@drawable/login_box_shape</item>
    </style>

</resources>
